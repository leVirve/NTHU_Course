 version: '2'
 services:
   web:
     build: .
     command: python manage.py runserver 0.0.0.0:8000 --settings=NTHU_Course.settings.dev_docker
     volumes:
       - .:/code
     ports:
       - "8000:8000"
